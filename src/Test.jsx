import { Box, CircularProgress, LinearProgress } from '@mui/material';

const Test = () => {
  return (
    <div>
      <Box>
        <CircularProgress color='secondary' />
        <LinearProgress />
      </Box>
    </div>
  );
};
export default Test;
